main = MainUI()

data_dict = {
    "type": "object",
    "key": "employee",
    "role": "null",
    "data": {
        "email": {"type": "string"},
        "password": {"type": "string"},
    },
}

# Svona kallarðu loggarðu inn:

# 1. formatta data_dict sem json

data = json.dumps(data_dict)

# 2. Kalla á LogicAPI.authenticate_employee og gefa honum json-data

response = LogicAPI.authenticate_employee(data) # <- þetta er json strengur

# 3. response_dict = json.loads(response) # <- þetta er dictionary

main.id = response_dict['data']['id']
main.role = response_dict['data']['role']
main.name = response_dict['data']['name']
main.isloggedin = True